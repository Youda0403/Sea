# 테스트 보고서 — 2026-09-21

## 실제 실행

`npm test`: 11 tests / 11 pass / 0 fail.

- 콘텐츠 참조와 결과 테이블 정합성
- 캐릭터 입력/중복 키워드 거부
- 고정 시드 재현 및 입력 상태 불변
- 성격·가치관·과거에 따른 충돌 점수 차이
- 거부 → 대체 행동 → 기억 → 일기 → JSON 왕복
- 강제 명령 후 실패, 안정도 −12, 1회 차감, 사실 일기
- 결과 6종 도달 가능
- 중복 행동, 아이템 부족, 전력 부족, 미확인 강제 명령 차단
- 거부 수용 후 철수 시 최초 의도 보존
- 손상/미래 버전 저장 거부
- 10,000회 시드 시뮬레이션에서 수치 범위와 기록 사실성 유지

## Chromium 실제 브라우저 자동화

`tests/browser.mjs`: 다음 9개 체크 모두 통과.

1. 캐릭터 생성 입력 및 초기 저장, 특수문자 이름 안전 렌더링
2. 거부 → 새로고침 → 대안 → 기억/일기 → 새로고침 동일 상태
3. 확인 대화상자 → 강제 명령 → 실패 → 실패 일기, 모듈 0·안정도 88
4. 320/360/390/768px에서 4탭 가로 넘침 없음
5. JSON 파일 가져오기 후 동일 상태 및 내비게이션 조작 가능
6. 수용 → 구조 성공 → 일기
7. 두 탭의 오래된 상태 쓰기 거부, 출발 비용 중복 차감 없음
8. IndexedDB put 실패 주입 시 트랜잭션 취소 및 기존 저장 보존
9. 브라우저 JavaScript 오류 0건

테스트 환경: Node.js 24, Playwright + 설치된 headless Chromium. Playwright 기본 브라우저 다운로드는 타임아웃되어 기존 Chromium 실행 파일을 사용. 테스트에 기록된 시드: 거부 1, 강제 실패 54, 수용 성공 2820.

## 시각 검사 및 미검증

390px 전체 페이지 스크린샷 확인. 테스트 시스템에 한국어 글꼴이 없어 한글 글리프가 네모로 렌더링됨. 이 환경에서는 한국어 줄바꿈/폰트의 최종 시각 품질을 확정하지 않음. 자동화 DOM의 한국어 레이블·텍스트·선택 결과 검증은 통과.

실물 Android Chrome/삼성 인터넷/iOS Safari, 스크린리더, 저장 공간 실제 고갈, 브라우저 강제 종료 순간, 배포된 GitHub Pages 환경은 아직 확인하지 않음. 오프라인 페이지 재접속은 미구현.

## 플레이 로그 요약

| 시드 | 선택 | 판단/후속 | 실제 행동 | 결과 | 일기 |
|---|---|---|---|---|---|
| 2820 | 직접 구조 | 수용 또는 주저 후 수용 | 직접 구조 | 성공 | 류 구조 사실 |
| 1 | 직접 구조 | 거부 → 대안 | 드론 지원 | 원격 결과 테이블 | 확정된 드론 결과 사실 |
| 54 | 직접 구조 | 거부 → 강제 | 직접 구조 | 실패 | 실패 사실 + 강제 동작 회고 |

최초 선택이 직접 구조였어도 드론을 선택하면 실제 행동은 `remote_rescue`. 강제 실패는 `forcedOverrideUsed=true`, `result=failure`. 일기는 선택 의도가 아니라 이 확정 결과를 사용.
